import React from "react";
import FormWrapper from "./components/FormWrapper/FormWrapper";

const App: React.FC = () => {
    return (
        <div>
            <FormWrapper />
        </div>
    );
};

export default App;
